# Dynamo DP Imbalance Rerun Log - 2026-05-22

Purpose: rerun the Lyris DP=4 EP workload after adding request-level backend
DP tracing, router admission fields, and SA-Bench time-series `/metrics`
scraping.

Scope:

- Cluster: Lyris.
- Hardware target: GB200, 4 GPUs on one node.
- Primary recipe: `qwen3-235b-a22b-vllm-agg-lyris-gb200-dp4-ep-round-robin.yaml`.
- Follow-up recipes, if the baseline starts cleanly:
  `qwen3-235b-a22b-vllm-agg-lyris-gb200-dp4-ep-load-aware.yaml` and
  `qwen3-235b-a22b-vllm-agg-lyris-gb200-dp4-ep-dedicated-router.yaml`.

Code under test:

- srt-slurm: `e5e7b5f563837a590c31a43c3cfa0cb8ef5ebb16`
  (`fix(sa-bench): cap peak throughput tokenization`), which includes the prior
  source-install fixes and SA-Bench metrics scrape support.
- Dynamo worktree branch: `request-trace-dp-events`.
- Dynamo commit: `732e31b7516342b4342a23765779bf6a6f88b892`
  (`fix(vllm): trace handler DP request lifecycle`), which includes the direct
  JSONL request trace writer and router admission trace support.
- Job `1871992` used `effd38d0e783ba6c9a3b4fc53413baa23f961fb1`; job `1873079`
  uses `37c2ce5bc42bc1442d99c0fe3eb2e8fe57be61ef`. Follow-up recipes now use
  `732e31b7516342b4342a23765779bf6a6f88b892` because the active vLLM backend
  path on Lyris is `handlers.py`, not only `llm_engine.py`.

Filesystem policy:

- Do not scan `/home`.
- Probe only explicit Lyris paths with `ls`, `stat`, or bounded log reads.
- Use the known model path from the previous successful run unless it fails:
  `/lustre/share/coreai_dlfw_dev/models/Qwen3-235B-A22B-Instruct-2507-FP4`.

## Step Log

- 2026-05-22 21:32 PT: Started rerun setup from local repo
  `/home/connorc/work/srt-slurm`; both local worktrees were clean and pushed.
- 2026-05-22 21:33 PT: Confirmed Lyris MFA/SSH socket is active with
  `squeue -u $USER`; no current user jobs were listed.
- 2026-05-22 21:33 PT: Verified the explicit shared model path exists on
  Lyris with `ls -ld`; no broad filesystem search was used.
- 2026-05-22 21:34 PT: First `srtctl dry-run` reached Lyris but failed during
  config validation because the cluster-side srtctl install did not know the
  new `benchmark.request_trace`, `benchmark.metrics_scrape`, or
  `benchmark.metrics_scrape_interval_s` fields. Next action is to update the
  Lyris scratch checkout to the fork commit before submitting.
- 2026-05-22 21:36 PT: Submitted a short zero-GPU Lyris maintenance session
  `srt-lyris-maint` as job `1871203` to run the scratch checkout update through
  MCP-managed SLURM access, because direct local SSH is blocked by the sandbox.
- 2026-05-22 21:40 PT: The cluster checkout was on old upstream commit
  `cb2fd2634f84db84c70aca2249b6724f4bb7c7c2`. Its `origin/main` diverged from
  the fork, so instead of hard-resetting `main`, switched the scratch worktree to
  a new branch `rerun-dp-imbalance-20260522` tracking the fork and verified HEAD
  `1c5b024166ee9a46b5cf3168e3baf04ba00c69a6`.
- 2026-05-22 21:43 PT: `srtctl dry-run` for the Lyris round-robin recipe passed
  with the patched srt-slurm checkout.
- 2026-05-22 21:45 PT: Updated the staged Dynamo recipes to install Dynamo from
  hash `f14399b89ed33f45775308c911c67ebfb3380a05` at job startup. This is
  required because the public runtime image alone does not contain the new Rust
  router admission trace fields.
- 2026-05-22 21:44 PT: Re-ran `srtctl dry-run` for the updated round-robin recipe;
  dry-run passed. Released maintenance session `srt-lyris-maint` and cancelled
  helper job `1871203`.
- 2026-05-22 21:45 PT: First `srtctl apply` did not submit a benchmark job.
  Pre-flight failed because the updated Lyris scratch checkout was missing
  generated setup artifacts: `configs/nats-server`, `configs/etcd`, and
  `bin/uv`. Next action is to run `make setup ARCH=aarch64` for the scratch
  checkout, then retry apply.
- 2026-05-22 21:47 PT: Submitted short zero-GPU Lyris setup session
  `srt-lyris-setup` as job `1871548` to run `make setup ARCH=aarch64`.
- 2026-05-22 21:49 PT: `make setup ARCH=aarch64` completed and restored
  `configs/nats-server`, `configs/etcd`, and `bin/uv`. Verified those exact
  paths with `ls -l`, then released setup session `srt-lyris-setup` and
  cancelled helper job `1871548`.
- 2026-05-22 21:48 PT: Submitted the instrumented round-robin benchmark.
  Job ID: `1871561`.
  Logs: `/lustre/fsw/coreai_dlfw_dev/connorc/srt-slurm/outputs/1871561/logs`.
- 2026-05-22 21:51 PT: Cancelled job `1871561`. Backend worker logs confirmed
  `Installing dynamo from source
  (f14399b89ed33f45775308c911c67ebfb3380a05, /configs cache)...`, but Rust
  bootstrap failed because the container ran as root while `HOME` was
  `/home/dynamo`: `error: $HOME differs from euid-obtained home directory`.
  Next action is to patch the srt-slurm hash source-install command to set
  root-owned Rust/Cargo homes before invoking rustup.
- 2026-05-22 21:52 PT: Patched srt-slurm source install to export
  `HOME=/root`, `RUSTUP_HOME=/root/.rustup`, `CARGO_HOME=/root/.cargo`, and
  prepend `/root/.cargo/bin` before Rust bootstrap. Committed and pushed
  srt-slurm `766e7ccb2b466b7eb9ebd09dc2742a64ffbe2d3d`.
- 2026-05-22 21:54 PT: Fast-forwarded the Lyris scratch checkout to
  `766e7ccb2b466b7eb9ebd09dc2742a64ffbe2d3d` via helper session
  `srt-lyris-sync` job `1871564`, then released the helper session.
- 2026-05-22 21:54 PT: Resubmitted the instrumented round-robin benchmark.
  Job ID: `1871578`.
  Logs: `/lustre/fsw/coreai_dlfw_dev/connorc/srt-slurm/outputs/1871578/logs`.
- 2026-05-22 21:58 PT: Cancelled job `1871578`. The root-home source install
  progressed into the Rust build, but compilation failed in
  `lib/mocker/src/replay/offline/components/router.rs` because one
  `SchedulingRequest` initializer outside the earlier search scope was missing
  `queued_at`. Patched Dynamo and pushed
  `6316855f40c099426e9b96c0d4ba3d7cf8d7b536`, then updated the staged recipes
  to install that hash.
- 2026-05-22 22:00 PT: Resubmitted the instrumented round-robin benchmark after
  the replay initializer fix, intending to use Dynamo `6316855f40`.
  Job ID: `1871789`.
  Logs: `/lustre/fsw/coreai_dlfw_dev/connorc/srt-slurm/outputs/1871789/logs`.
- 2026-05-22 22:03 PT: Cancelled job `1871789`. Source install failed at
  `git checkout` because the recipe used an incorrect full hash suffix
  (`6316855f405be...`) even though the short prefix was right. Verified the
  remote branch `dev/connorc/request-trace-dp-events` resolves to
  `6316855f40c099426e9b96c0d4ba3d7cf8d7b536`, then corrected the recipes.
- 2026-05-22 22:04 PT: Resubmitted the instrumented round-robin benchmark with
  exact Dynamo hash `6316855f40c099426e9b96c0d4ba3d7cf8d7b536`.
  Job ID: `1871831`.
  Logs: `/lustre/fsw/coreai_dlfw_dev/connorc/srt-slurm/outputs/1871831/logs`.
- 2026-05-22 22:12 PT: Cancelled job `1871831`. Source install progressed
  further but Rust compilation failed in `lib/llm/src/kv_router/push_router.rs`
  because two `FindBestMatchOutcome::Routed` destructuring patterns did not
  ignore the newly added router admission trace fields. Patched Dynamo and
  pushed `effd38d0e783ba6c9a3b4fc53413baa23f961fb1`, then updated all staged
  recipes to install that exact hash.
- 2026-05-22 22:13 PT: Resubmitted the instrumented round-robin benchmark with
  exact Dynamo hash `effd38d0e783ba6c9a3b4fc53413baa23f961fb1`.
  Job ID: `1871874`.
  Logs: `/lustre/fsw/coreai_dlfw_dev/connorc/srt-slurm/outputs/1871874/logs`.
- 2026-05-22 22:22 PT: Cancelled job `1871874`. The patched Rust build
  completed (`Finished dev profile`) and reached `maturin` packaging, but wheel
  creation failed because `patchelf` was missing in the runtime container:
  `Failed to execute 'patchelf'`. Patched srt-slurm source install to install
  `patchelf` alongside `libclang-dev`, `git`, and `protobuf-compiler`.
- 2026-05-22 22:24 PT: Fast-forwarded the Lyris scratch checkout to
  `a8e3527d7d1bc5dc4336baa063fa612bbd72146c` via helper session
  `srt-lyris-sync-patchelf` job `1871906`, then released the helper session.
- 2026-05-22 22:24 PT: Resubmitted the instrumented round-robin benchmark after
  the srt-slurm `patchelf` source-install fix.
  Job ID: `1871908`.
  Logs: `/lustre/fsw/coreai_dlfw_dev/connorc/srt-slurm/outputs/1871908/logs`.
- 2026-05-22 22:38 PT: Job `1871908` reached a healthy server with all four
  decode DP ranks registered (`Have 0 prefills and 4 decodes`) and started
  SA-Bench, but the benchmark failed before load generation. Root cause:
  the staged recipes omitted the original baseline's `use_chat_template: false`;
  with the default `true`, SA-Bench subtracted the Qwen chat-template overhead
  from `isl=2` and failed request sampling with `ValueError: low >= high`.
  Restored `use_chat_template: false` across all staged recipes.
- 2026-05-22 22:39 PT: Resubmitted the instrumented round-robin benchmark after
  restoring `use_chat_template: false`; source wheel cache is warm.
  Job ID: `1871992`.
  Logs: `/lustre/fsw/coreai_dlfw_dev/connorc/srt-slurm/outputs/1871992/logs`.
- 2026-05-22 22:46 PT: Job `1871992` reached a healthy server on `lyris0213`
  with all four decode DP ranks registered (`Have 0 prefills and 4 decodes`)
  and launched SA-Bench with `use_chat_template=False`.
- 2026-05-22 22:58 PT: The SA-Bench warmup pass completed 16,384 requests at
  request-rate 250. This is not the final measured trace pass, but it is a useful
  early signal: 37,175.82 output tok/s, 36.30 req/s, mean TTFT 650.33 ms,
  p99 TTFT 4,287.34 ms, mean TPOT 30.33 ms, and mean ITL 35.58 ms.
- 2026-05-22 22:58 PT: The measured instrumented pass started with
  `--request-rate inf`, `--num-prompts 81920`, request trace output
  `/logs/sa-bench_isl_2_osl_1024/request_trace_concurrency_8192_gpus_4.jsonl`,
  and metrics scrape output
  `/logs/sa-bench_isl_2_osl_1024/metrics_trace_concurrency_8192_gpus_4/index.jsonl`.
- 2026-05-22 23:04 PT: Confirmed metrics scrape is active. Example scrape
  `frontend__300.prom` contained `dynamo_frontend_inflight_requests=7857`,
  `dynamo_frontend_queued_requests=5592`, request-plane queue/send/TTFT
  histograms, and `dynamo_router_overhead_scheduling_ms` with zero samples.
  `grep -c dynamo_request_trace` returned 0 for both frontend and backend logs,
  so the log-target trace path is not reliable for this run.
- 2026-05-22 23:08 PT: Observed the measured pass in progress. Backend queue
  depth showed temporal imbalance waves: some samples had all ranks at
  `Running: 864`, while later samples had rank-specific divergence such as
  Engine 002/003 waiting over 100 requests while Engine 000/001 had lower or no
  waiting queue. This run should still produce client request trace and metrics
  data, but not joined server-side request lifecycle events.
- 2026-05-22 23:09 PT: Patched Dynamo to write request-trace events directly to
  `/logs/dynamo_request_trace_router_<pid>.jsonl` and
  `/logs/dynamo_request_trace_vllm_<pid>.jsonl` when
  `DYN_REQUEST_TRACE_LOGGING=1`, while preserving the existing log lines. Pushed
  Dynamo `37c2ce5bc42bc1442d99c0fe3eb2e8fe57be61ef` to
  `dev/connorc/request-trace-dp-events` and updated all staged recipes to use
  that hash for the next rerun.
- 2026-05-22 23:20 PT: Job `1871992` was still running. Frontend log samples
  showed completed measured-pass requests with TTFTs around 180-186 s and
  elapsed latencies around 186-190 s. Backend samples at the same stage showed
  `Waiting: 0` on all ranks while `Running` counts continued to diverge by rank,
  with examples ranging from roughly 130 to 860 running requests. This is a
  stronger temporal-imbalance signal than final request counts alone.
- 2026-05-22 23:23 PT: Metrics scrape remained active. The latest scrape index
  entry was `frontend__1461.prom`, and `index.jsonl` had 1,466 entries. The
  SA-Bench output directory still contained only the metrics scrape directory,
  so the measured `results_*.json` and client `request_trace_*.jsonl` files had
  not yet been flushed.
- 2026-05-22 23:36 PT: Frontend `request received` and `request completed`
  counts both reached 98,307, matching the warmup pass, measured pass, and
  probe requests. Backend logs showed all ranks drained to `Running: 0,
  Waiting: 0` by about 23:34 PT.
- 2026-05-22 23:38 PT: Job `1871992` was still allocated even though backend
  serving had drained. The only active benchmark client process was
  `benchmark_serving.py`, using roughly 93% CPU and 8.1 GB RSS, with no
  `results_*.json` or request-trace JSONL opened yet. This points to
  client-side post-run metric computation, not backend serving.
- 2026-05-22 23:42 PT: Identified a likely SA-Bench bottleneck in
  `calculate_metrics`: peak output throughput reconstruction tokenizes every
  streamed text chunk after the run. For this workload, that can mean tens of
  millions of tokenizer calls after all backend requests have completed. Patched
  srt-slurm to use exact per-chunk tokenization only for small runs and a
  proportional output-token approximation for large runs. Local
  `python -m py_compile` and `git diff --check` passed.
- 2026-05-22 23:44 PT: Committed and pushed srt-slurm
  `e5e7b5f563837a590c31a43c3cfa0cb8ef5ebb16`
  (`fix(sa-bench): cap peak throughput tokenization`). Fast-forwarded the Lyris
  scratch checkout branch `rerun-dp-imbalance-20260522` to the same commit.
- 2026-05-22 23:46 PT: Cancelled stuck job `1871992` after confirming it had
  reached 98,307 received and completed frontend request lines, all backend
  ranks had drained, and the remaining `benchmark_serving.py` process was
  post-processing on CPU. This attempt did not produce final `results_*.json`
  or client request-trace JSONL files, but it did preserve frontend/backend logs
  and metrics scrape snapshots under
  `/lustre/fsw/coreai_dlfw_dev/connorc/srt-slurm/outputs/1871992/logs`.
- 2026-05-22 23:47 PT: Submitted patched round-robin rerun job `1873079` with
  tags `dp-imbalance,round-robin,trace-rerun,sa-bench-fast-peak`. Logs:
  `/lustre/fsw/coreai_dlfw_dev/connorc/srt-slurm/outputs/1873079/logs`.
- 2026-05-23 00:00 PT: Job `1873079` became healthy on `lyris0236` with four
  decode DP ranks registered and started SA-Bench. The benchmark command is
  running the same 16,384-request warmup at request-rate 250 before the measured
  `concurrency=8192` pass.
- 2026-05-23 00:03 PT: Confirmed this rerun is using the patched srt-slurm
  client and `use_chat_template=False`. Backend vLLM logs are advancing with all
  four DP ranks active and `Waiting: 0` in the sampled warmup window. Running
  request counts are close but not identical across ranks; examples at this
  stage were generally in the 200-280 range per rank.
- 2026-05-23 00:04 PT: Server-side request trace output has not appeared yet:
  no `/logs/dynamo_request_trace_*.jsonl` files were present in the job log
  directory, and `grep -c dynamo_request_trace` returned 0 for both frontend and
  backend logs. The client request trace and Prometheus metrics scrape remain
  enabled for the measured pass, so the run is still useful even if the direct
  Dynamo JSONL hook needs another patch.
- 2026-05-23 00:12 PT: Confirmed the missing direct backend JSONL trace is
  caused by hook placement: the Lyris `python -m dynamo.vllm` runtime is using
  the vLLM worker handlers in `components/src/dynamo/vllm/handlers.py`, while
  the previous backend lifecycle hook was only in `llm_engine.py`. Patched and
  pushed Dynamo `732e31b7516342b4342a23765779bf6a6f88b892`
  (`fix(vllm): trace handler DP request lifecycle`) to log
  `backend_dp_enter`, `backend_dp_first_output`, `backend_dp_first_token`, and
  `backend_dp_done` from the active handler path. Updated follow-up recipes to
  use this hash; the already-running job `1873079` will not pick it up.
- 2026-05-23 00:10 PT: Job `1873079` completed the 16,384-request warmup pass
  and started the measured pass with `--request-rate inf`,
  `--num-prompts 81920`, `--max-concurrency 8192`, client request trace output
  `/logs/sa-bench_isl_2_osl_1024/request_trace_concurrency_8192_gpus_4.jsonl`,
  and metrics scrape output
  `/logs/sa-bench_isl_2_osl_1024/metrics_trace_concurrency_8192_gpus_4/index.jsonl`.
  Warmup results: 33,727.73 output tok/s, 32.94 req/s, mean TTFT 479.18 ms,
  p99 TTFT 1,839.71 ms, mean TPOT 27.93 ms, mean ITL 30.97 ms, and mean E2EL
  29,048.83 ms.
- 2026-05-23 00:14 PT: Early measured-pass backend summary over 301 vLLM
  engine samples showed all ranks reaching `Running: 864` and `Waiting: 1184`.
  Per-rank running means were close (`403.5`-`416.3`), and per-rank waiting
  means were close (`225.1`-`243.6`). Temporal skew was still visible in short
  windows: max waiting skew 938 requests, max running skew 32 requests, and max
  generation-throughput skew 2,271 tok/s. This looks more like queue-wave /
  temporal admission behavior than a persistent final request-count imbalance.
- 2026-05-23 00:15 PT: Added `metrics_summary.py` to summarize Prometheus
  snapshots. The measured pass had 440 frontend scrapes at the sample point:
  `dynamo_frontend_inflight_requests` mean 7,527 and p50/p95/max 8,192;
  `dynamo_frontend_queued_requests` mean 4,535, p95 6,006, max 6,912;
  `dynamo_request_plane_send_seconds` window mean 6.46 ms;
  `dynamo_request_plane_queue_seconds` window mean 309 ms; and
  `dynamo_request_plane_roundtrip_ttft_seconds` window mean 114.33 s.
  `dynamo_router_overhead_scheduling_ms` still had zero samples. No explicit
  forward-pass state freshness or slot-age metric was exposed in the scrape;
  only generic frontend stage metrics matched the freshness search terms.
- 2026-05-23 00:22 PT: A longer backend summary over 493 engine samples showed
  the temporal imbalance signal strengthening after the initial saturated
  waiting-queue wave. Per-rank averages stayed close (`running_mean` 519.2 to
  531.9 and `waiting_mean` 161.0 to 172.1), but temporal skew grew:
  `running_skew` mean 43.7, p95 363, max 603; `waiting_skew` mean 18.0, p95
  65, max 938; generation-throughput skew max 25,445 tok/s. This supports the
  hypothesis that final/even request distribution can coexist with large
  time-window skew and rank-dependent draining.
- 2026-05-23 00:24 PT: Metrics summary at 741 frontend scrapes showed
  `dynamo_request_plane_queue_seconds_count=45697` and
  `dynamo_request_plane_roundtrip_ttft_seconds_count=40957`, so the measured
  pass was roughly halfway through request-plane admission/completion. Inflight
  remained pinned near 8,192 (`last=8189`), frontend queued remained high
  (`last=4740`, max 7105), and roundtrip TTFT window mean increased to
  127.99 s while request-plane send stayed small at 16.7 ms.
- 2026-05-23 00:29 PT: Backend tail showed the original failure mode directly:
  backend `Waiting` frequently dropped to zero or near zero while frontend
  inflight remained pinned near 8,192 and backend `Running` diverged by hundreds
  of requests across DP ranks. Examples included ranks in the same window near
  `Running: 837/652/652/654` and later `Running: 641/338/262/385`.
  Metrics at 1,115 scrapes showed request-plane admission count 59,784,
  roundtrip-TTFT count 54,779, frontend inflight last 8,191, frontend queued
  last 5,006, and roundtrip TTFT window mean 138.56 s.
- 2026-05-23 00:32 PT: Job `1873079` was still running on `lyris0236`; the
  final measured `results_*.json` and client request-trace JSONL files had not
  yet appeared in the SA-Bench output directory. Live metrics at 1,325 frontend
  scrapes showed `dynamo_frontend_inflight_requests` mean 7,874.67, p50 8,184,
  p95/max 8,192, last 8,181; `dynamo_frontend_queued_requests` mean 5,153.7,
  p50 5,042, p95 6,326, max 7,105, last 5,368; request-plane queue window mean
  124.5 ms; request-plane send window mean 26.8 ms; and request-plane
  roundtrip TTFT window mean 139.63 s. `dynamo_router_overhead_scheduling_ms`
  still had zero samples.
- 2026-05-23 00:32 PT: Backend summary over 731 vLLM engine samples showed
  close aggregate per-rank means but large temporal skew. Per-rank
  `running_mean` ranged from 552.7 to 562.7, and per-rank `waiting_mean` ranged
  from 111.7 to 119.4. Window skew was much larger: `running_skew` mean 95.8,
  p95 431, max 603; `waiting_skew` mean 14.4, p95 55, max 938; and generation
  throughput skew mean 4,498 tok/s, p95 22,514 tok/s, max 38,585 tok/s. This
  continues to support temporal imbalance or request-slot refresh lag rather
  than a simple aggregate DP-rank count imbalance.
- 2026-05-23 00:35 PT: Job `1873079` remained in the measured pass. The
  SA-Bench output directory still contained only the metrics scrape directory,
  so final result JSON and client trace JSONL were still pending. Metrics at
  1,502 scrapes showed `dynamo_frontend_inflight_requests` mean 7,897.91, p50
  8,180, p95/max 8,192, last 7,635; `dynamo_frontend_queued_requests` mean
  5,216.96, p50 5,205, p95 6,343, max 7,105, last 5,713; request-plane queue
  window mean 113.2 ms; send window mean 28.4 ms; and roundtrip TTFT window
  mean 141.88 s. Backend summary over 805 engine samples still had close
  per-rank aggregates (`running_mean` 557.3 to 562.2, `waiting_mean` 101.3 to
  108.1), but temporal skew remained high: `running_skew` mean 108.3, p95 450,
  max 603; `waiting_skew` max 938; and generation-throughput skew max
  38,585 tok/s.
- 2026-05-23 00:38 PT: Frontend and backend logs confirmed the run was still
  actively serving, not stuck in post-processing. Targeted frontend log counts
  showed about 86,042 `request received` lines and 77,624 `request completed`
  lines, so the run had not yet reached the expected warmup+measured total.
  Sample completions in the frontend tail had elapsed latencies around
  159-160 s and TTFT around 91-93 s. Backend samples in the same window showed
  `Waiting: 0` on all DP ranks while `Running` varied sharply, including
  samples near 130 requests on one rank and 600+ on another. This is a direct
  live instance of backend waiting draining to zero while frontend admission
  continues and DP ranks have different in-flight work.
- 2026-05-23 00:42 PT: The measured pass was near the expected total admission
  count but had not finished. Targeted frontend log counts showed about 93,293
  `request received` lines and 84,787 `request completed` lines. The SA-Bench
  output directory still contained only the metrics scrape directory. Metrics
  at 1,887 scrapes showed frontend inflight last 7,865 and queued last 6,521;
  request-plane queue window mean 88.9 ms, send window mean 32.0 ms, and
  roundtrip TTFT window mean 143.06 s.
- 2026-05-23 00:44 PT: Frontend admission reached the expected
  warmup+measured+probe total: 98,307 `request received` lines. Completions had
  reached 91,166, so the run was still draining and final result files were not
  yet present. Backend summary over 1,016 engine samples showed aggregate
  per-rank means still close (`running_mean` 528.9 to 533.2, `waiting_mean`
  80.6 to 85.9), while temporal skew persisted: `running_skew` mean 131.4,
  p95 452, max 603; `waiting_skew` max 938; generation-throughput skew mean
  5,599 tok/s, p95 22,979 tok/s, max 38,585 tok/s.
- 2026-05-23 00:47 PT: All 98,307 frontend requests had completed and backend
  queues drained to zero. Result and trace artifacts appeared under
  `/lustre/fsw/coreai_dlfw_dev/connorc/srt-slurm/outputs/1873079/logs/sa-bench_isl_2_osl_1024`:
  `results_concurrency_8192_gpus_4.json` (148 MB) and
  `request_trace_concurrency_8192_gpus_4.jsonl` (86 MB).
- 2026-05-23 00:48 PT: SA-Bench printed the final measured result for job
  `1873079`: 81,920 successful requests, 2,188.34 s duration, 37.43 req/s,
  38,333.23 output tok/s, mean TTFT 147,095.77 ms, median TTFT 148,747.01 ms,
  p99 TTFT 258,153.19 ms, mean TPOT 62.15 ms, and mean ITL 89.87 ms. The final
  frontend metrics summary had 2,204 scrapes with inflight/queued last 0,
  `dynamo_request_plane_queue_seconds_count=98307`, queue window mean 84.1 ms,
  `dynamo_request_plane_send_seconds` window mean 30.9 ms, and
  `dynamo_request_plane_roundtrip_ttft_seconds` window mean 143.02 s.
- 2026-05-23 00:48 PT: Client trace summary found 245,760 client events for
  81,920 measured requests and a client TTFT distribution matching SA-Bench
  (mean 147.095776 s, p50 148.747074 s, p95 199.332955 s, max 268.157778 s).
  Server trace events were 0 because this job used Dynamo
  `37c2ce5bc42bc1442d99c0fe3eb2e8fe57be61ef`, which missed the active
  `components/src/dynamo/vllm/handlers.py` path on Lyris. The follow-up
  round-robin recipe now installs
  `732e31b7516342b4342a23765779bf6a6f88b892`, which adds backend lifecycle
  trace events in that handler path.
- 2026-05-23 00:48 PT: Final backend log summary over 1,065 engine samples
  showed close aggregate rank means (`running_mean` 521.0 to 524.8 and
  `waiting_mean` 76.7 to 81.7) but large time-window imbalance:
  `running_skew` mean 137.3, p50 88, p95 452, max 603; `waiting_skew` max 938;
  and generation-throughput skew mean 5,711.9 tok/s, p95 22,514.3 tok/s, max
  38,585.4 tok/s.
- 2026-05-23 00:48 PT: Sweep completed successfully. `srtctl_status` reported
  the parent job completed with exit code 0; cancelled service substeps are
  expected from orchestrator cleanup.
